<!DOCTYPE html>

<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    <title>Sign in</title>
    <meta name="title" content="@@title">
    <meta name="description" content="@@description">
    <meta property="og:type" content="website">
    <meta property="og:title" content="@@title">
    <meta property="og:description" content="@@description">
    <meta property="og:image" content="@@image">
    <meta property="og:url" content="@@url">
    <meta name="twitter:image" content="@@image">
    <meta name="twitter:card" content="summary_large_image">
    <link rel="icon" href="/images/favicon.svg" sizes="32x32">
    <link rel="apple-touch-icon" href="/images/favicon.svg">
    <link rel="stylesheet" href="/css/main.css">
    <script src="/js/main.js" defer></script>
  </head>

  <body class="text-xs">
    <section class="min-h-screen p-4 sm:p-10 flex items-center justify-center">
      <div class="w-70">
        <h1 class="font-bold text-sm text-center">Sign in</h1>
        <form method="post" action="/auth/signin" id="auth-form">
          <div class="mt-4 w-full relative">
            <label for="email" class="inline-block mb-1 font-bold">Email</label>
            <input id="email" name="email" type="text" value="<%= user.email || '' %>" placeholder="jane@mail.com" class="w-full px-3 py-1.5 border border-zinc-200 bg-white rounded-md relative focus-visible:z-10" required />
            <span class="field-error"><%= errors.find(error => error.name === 'email')?.message || '' %></span>
          </div>
          <div class="mt-4 w-full relative">
            <label for="password" class="inline-block mb-1 font-bold">Password</label>
            <input id="password" name="password" type="password" value="<%= user.password || '' %>" placeholder="· · · · · · · · · ·" class="w-full px-3 py-1.5 border border-zinc-200 bg-white rounded-md relative focus-visible:z-10" required />
            <span class="field-error"><%= errors.find(error => error.name === 'password')?.message || '' %></span>
          </div>
          <div class="mt-8">
            <button type="submit" class="w-full px-3 py-1.5 border border-zinc-200 bg-zinc-100 rounded-md cursor-pointer relative focus-visible:z-10">Sign in</button>
          </div>
          <div class="mt-4 text-center">
            <p>Don't have an account? <a href="/auth/signup" class="font-bold hover:underline">Register</a></p>
          </div>
        </form>
      </div>
    </section>
  </body>

</html>